你资深开发工程师，
请你帮我开发一个基于python和tk的文档处理图形界面应用，实现将指定目录结构下的最深一级子目录中的所有图片生成一个二维码图片，并将二维码图片生成到一个pdf中。
给用户选择是乡还是村，乡是三级，村是二级目录结构。生成的二维码图片命名为“目录名_qr.png”，并保存在对应的最深一级子目录中。
随后把这个图片插入到pdf中指定位置中，pdf的尺寸可以选择A3、A4、A5或自定义尺寸，二维码图片插入位置可以选择，根据图片左上角的坐标来确定，让用户自行调整即可，二维码图片大小也由用户输入进行确定，pdf文件命名为“目录名_qr.pdf”，并保存在对应的最深一级子目录中。

其他人可以通过扫这个二维码来访问该目录下的所有图片，这些图片我准备按目录结构存在阿里云的oss上，意味着你要帮我把文件通过python上传到oss，二维码内容为该目录在oss上的访问链接。
最后，请你帮我提交到github actions生成windows可执行文件。

根目录下“西沟乡麻地沟村（资料扫描）”是测试文件夹。
在我目录下创建一个.venv虚拟环境，开始你的开发吧。
